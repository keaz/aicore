import std.io;

fn abs(x: Int) -> Int
ensures result >= 0 {
    if x >= 0 { x } else { 0 - x }
}

fn emit(v: Int) -> Int effects { io }
requires v >= 0
ensures result >= 0 {
    print_int(v);
    v
}

fn main() -> Int effects { io } {
    emit(abs(-42));
    0
}
