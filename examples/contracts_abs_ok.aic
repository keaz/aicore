module examples.contracts_abs_ok;
import std.io;

fn abs(x: Int) -> Int ensures result >= 0 {
    if x >= 0 { x } else { 0 - x }
}

fn main() -> Int effects { io } {
    let v = abs(-7);
    print_int(v);
    0
}
