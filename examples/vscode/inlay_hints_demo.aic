module examples.vscode.inlay_hints_demo;

import std.io;

fn checked(x: Int) -> Int effects { io } requires x >= 0 ensures result >= 0 {
    x
}

fn main() -> Int effects { io } {
    let seed = 41;
    let value = checked(seed);
    print_int(value + 1);
    0
}
