module examples.vscode.status_bar_demo;

import std.io;

fn compute(seed: Int) -> Int {
    seed + 1
}

fn main() -> Int effects { io } capabilities { io } {
    let value = compute(41);
    print_int(value);
    0
}
