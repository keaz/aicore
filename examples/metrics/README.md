# Metrics Example

Run:

```bash
cargo run --quiet --bin aic -- metrics examples/metrics/src/main.aic
```

Gate with configured thresholds:

```bash
cargo run --quiet --bin aic -- metrics examples/metrics/src/main.aic --check
```
