module examples.ops.sandbox_profiles.fs_blocked_demo;

import std.fs;

fn main() -> Int effects { fs } {
    let result = fs.read_text("Cargo.toml");
    match result {
        Ok(_) => 1,
        Err(err) => match err {
            PermissionDenied() => 0,
            _ => 2,
        },
    }
}
