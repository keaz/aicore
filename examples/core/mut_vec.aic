import std.io;
import std.vec;

fn grow(v: Vec[Int]) -> Vec[Int] {
    vec.push(v, 2)
}

fn main() -> Int effects { io } capabilities { io } {
    let mut v: Vec[Int] = vec.new_vec();
    v = vec.push(v, 1);
    v = grow(v);
    print_int(vec_len(v));
    0
}
