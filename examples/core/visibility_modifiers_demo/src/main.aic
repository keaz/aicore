module visibility_demo.main;
import visibility_demo.api;
import std.io;

fn main() -> Int effects { io } {
    let record = api.make_record(20);
    let score = api.public_add(record.visible, api.crate_value());
    print_int(score);
    0
}
