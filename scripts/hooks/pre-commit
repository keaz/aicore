#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT_DIR"

if ! make ci-fast; then
  echo "pre-commit failed: run 'make ci-fast' and fix issues." >&2
  exit 1
fi
