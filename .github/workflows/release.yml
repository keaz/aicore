name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-preflight:
    name: Release Preflight
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Install clang
        run: |
          sudo apt-get update
          sudo apt-get install -y clang

      - name: Full CI gate
        run: make ci

      - name: Release policy gate
        run: cargo run --quiet --bin aic -- release policy --check

      - name: Security audit gate
        run: cargo run --quiet --bin aic -- release security-audit --json

  release-build:
    name: Build Release (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: release-preflight
    env:
      AIC_SIGNING_KEY: ${{ secrets.AIC_SIGNING_KEY }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            bin_name: aic
            archive_ext: tar.gz
          - os: macos-latest
            bin_name: aic
            archive_ext: tar.gz
          - os: windows-latest
            bin_name: aic.exe
            archive_ext: zip

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build binary
        run: cargo build --release --locked --bin aic

      - name: Generate release metadata
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          cargo run --quiet --bin aic -- release manifest --root . --output dist/repro-manifest.json --source-date-epoch 1700000000
          cargo run --quiet --bin aic -- release sbom --root . --output dist/sbom.json --source-date-epoch 1700000000
          cargo run --quiet --bin aic -- release policy --check

      - name: Package (Unix)
        if: matrix.archive_ext == 'tar.gz'
        shell: bash
        run: |
          set -euo pipefail
          OS_NAME="$(echo "${RUNNER_OS}" | tr '[:upper:]' '[:lower:]')"
          ARCH_NAME="$(echo "${RUNNER_ARCH}" | tr '[:upper:]' '[:lower:]')"
          ARCHIVE="aicore-${GITHUB_REF_NAME}-${OS_NAME}-${ARCH_NAME}.tar.gz"

          mkdir -p dist
          cp "target/release/${{ matrix.bin_name }}" "dist/${{ matrix.bin_name }}"
          tar -C dist -czf "$ARCHIVE" "${{ matrix.bin_name }}"
          shasum -a 256 "$ARCHIVE" > "$ARCHIVE.sha256"

          echo "ARCHIVE=$ARCHIVE" >> "$GITHUB_ENV"

      - name: Package (Windows)
        if: matrix.archive_ext == 'zip'
        shell: pwsh
        run: |
          $osName = $env:RUNNER_OS.ToLower()
          $archName = $env:RUNNER_ARCH.ToLower()
          $archive = "aicore-$env:GITHUB_REF_NAME-$osName-$archName.zip"

          New-Item -ItemType Directory -Path dist -Force | Out-Null
          Copy-Item "target/release/${{ matrix.bin_name }}" "dist/${{ matrix.bin_name }}"
          Compress-Archive -Path "dist/${{ matrix.bin_name }}" -DestinationPath $archive -Force

          $hash = (Get-FileHash -Algorithm SHA256 $archive).Hash.ToLower()
          "$hash  $archive" | Out-File -FilePath "$archive.sha256" -Encoding ascii
          "ARCHIVE=$archive" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Sign provenance metadata
        if: env.AIC_SIGNING_KEY != ''
        shell: bash
        run: |
          set -euo pipefail
          cargo run --quiet --bin aic -- release provenance \
            --artifact "dist/${{ matrix.bin_name }}" \
            --sbom dist/sbom.json \
            --manifest dist/repro-manifest.json \
            --output dist/provenance.json \
            --key-env AIC_SIGNING_KEY \
            --key-id "github-actions-${{ runner.os }}-${{ runner.arch }}"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ runner.os }}-${{ runner.arch }}
          path: |
            ${{ env.ARCHIVE }}
            ${{ env.ARCHIVE }}.sha256
            dist/repro-manifest.json
            dist/sbom.json
            dist/provenance.json

  release-publish:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: release-build
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: release-*
          merge-multiple: true

      - name: Display files
        run: ls -la dist

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256
            dist/*.json
          generate_release_notes: true
